Producer
- produces the TCP stream to be consumed (i.e. a web server behind a firewall)
- Connects to consumers TCP server

Consumer
- consumes the TCP content. i.e. a browser
- Hosts a public TCP server which the producer connects to
- Hosts a local TCP server which receives data and sends to producer
